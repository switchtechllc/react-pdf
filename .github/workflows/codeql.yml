name: codeql
on:
  push:
    branches: [ main ]
  pull_request:
  schedule:
    - cron: '0 3 * * 1'
jobs:
  analyze:
    permissions:
      security-events: write
    uses: github/codeql-action/analyze@v3
    with:
      category: '/language:javascript'
